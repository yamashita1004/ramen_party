<div class="container">
  <div class="row">
    <div class="col-4 mx-auto mb-5 admin-customer-show">
<p>ユーザー投稿一覧</p>
<% @users.each do |user| %>
 <%= image_tag user.get_image, size: "200x200" %>
 <p>name</p>
  <p><%= link_to user.name, admin_user_path(user.id) %></p>

<% end %>
       </div>
     </div>
    </div>

